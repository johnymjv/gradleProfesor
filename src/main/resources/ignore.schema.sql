create table user
(
    id integer not null primary key auto_increment,
    full_name VARCHAR(255), 
    login VARCHAR(255),
    password VARCHAR(255), 
    email VARCHAR(20)
);


create table ciudad
(
    id integer not null primary key auto_increment,
    full_name VARCHAR(255), 
    login VARCHAR(255),
    password VARCHAR(255), 
    email VARCHAR(20)
);